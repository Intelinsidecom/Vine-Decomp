<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android" xmlns:vine="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">
    <co.vine.android.widget.CroppableImageView
        android:id="@+id/image"
        android:background="@android:color/black"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        vine:cropRectPadding="5dp"
        vine:cropRectStrokeColor="@color/solid_white"
        vine:cropShadowColor="#99000000"/>
    <LinearLayout
        android:id="@+id/toolbar"
        android:layout_alignParentBottom="true"
        style="@style/Toolbar">
        <Button
            android:id="@+id/cancel_button"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginLeft="3dp"
            android:text="@string/cancel"
            android:layout_weight="0.5"
            android:onClick="onClickHandler"
            style="@style/Button"/>
        <Button
            android:id="@+id/save_button"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginRight="3dp"
            android:text="@string/save"
            android:layout_weight="0.5"
            android:onClick="onClickHandler"
            style="@style/Button"/>
    </LinearLayout>
</RelativeLayout>
