<phone:PhoneApplicationPage
    x:Class="Vine.Pages.CropVideo.CropVideoPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
   xmlns:control="clr-namespace:Vine.Controls"
	FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="Portrait" Orientation="Portrait"
    mc:Ignorable="d"
	shell:SystemTray.ForegroundColor="#fffffe"
    shell:SystemTray.Opacity="0"
    shell:SystemTray.IsVisible="True">
    <phone:PhoneApplicationPage.Resources>
        <ResourceDictionary Source="CropControl.xaml"/>
    </phone:PhoneApplicationPage.Resources>
    <phone:PhoneApplicationPage.ApplicationBar>
        <shell:ApplicationBar BackgroundColor="Black"  ForegroundColor="White">
            <shell:ApplicationBarIconButton IconUri="/Assets/AppBar/cropout.png" IsEnabled="False" Text="auto fit" Click="AutoFit_Click"/>
            <shell:ApplicationBarIconButton IconUri="/Assets/AppBar/transport.pause.png" IsEnabled="True" Text="pause" Click="PlayPause_Click"/>
            <shell:ApplicationBarIconButton IconUri="/Toolkit.Content/ApplicationBar.Check.png" IsEnabled="True" Text="validate" Click="Validate_Click"/>
        </shell:ApplicationBar>
    </phone:PhoneApplicationPage.ApplicationBar>
    <!--LayoutRoot is the root grid where all page content is placed-->
    <Grid x:Name="LayoutRoot" Background="{StaticResource NoDarkStripeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>

        </Grid.RowDefinitions>

        <!--TitlePanel contains the name of the application and page title-->


        <!--ContentPanel - place additional content here-->
        <Grid>
            <Rectangle x:Name="AutoFitBorder" Height="440" Width="440" Visibility="Collapsed" StrokeDashArray="3,3" Fill="White"/>
            <Grid x:Name="ContentPanel" SizeChanged="ContentPanel_SizeChanged">
                <Canvas>
                    <Grid x:Name="MediaPanel" HorizontalAlignment="Center"  VerticalAlignment="Center">
                        <control:CropControl x:Name="CropControl" Opacity="0">
                            <MediaElement x:Name="Media" Stretch="Uniform"   CurrentStateChanged="Media_CurrentStateChanged" AutoPlay="true"/>
                        </control:CropControl>
                    </Grid>
                </Canvas>
            </Grid>
        </Grid>
        <StackPanel x:Name="BackgroundChooserPanel" Visibility="Collapsed" Grid.Row="1" Orientation="Horizontal" VerticalAlignment="Bottom" Margin="12,6,12,0">
            <Button Background="White" BorderThickness="0" Width="80" Height="80" Click="FitWhite_Click" />
            <Button Background="Black" BorderThickness="0" Width="80" Height="80" Click="FitBlack_Click"/>
        </StackPanel>
        <Grid Grid.Row="2" Margin="12,12,12,24" x:Name="TimespanPanel">
            <control:RangeSlider x:Name="CropTimeSlider"  Background="white" Maximum="100"
								 UpperValue="40"
								 MinimumInterval="3000"
								 MaximumInterval="15000"/>

        </Grid>
        <Grid x:Name="EncodingPanel" Background="Black"
				   Grid.RowSpan="3"
				   Visibility="Collapsed">
            <StackPanel VerticalAlignment="Center" >
                <TextBlock Text="{Binding  LocalizedResources.EncodingInProgress,Source={StaticResource LocalizedStrings}}"
                           Foreground="White" HorizontalAlignment="Center"/>
                <ProgressBar x:Name="EncodingProgress"  Foreground="{StaticResource PrincBrush}" Margin="12,4,12,0"/>
            </StackPanel>
            <Grid x:Name="EncodingPanelAd" VerticalAlignment="Bottom"/>
        </Grid>
    </Grid>

</phone:PhoneApplicationPage>