<phone:PhoneApplicationPage x:Class="Vine.AnalyzeUriPage"
                            xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                            xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                            xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
                            xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
                            xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                            xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                            FontFamily="{StaticResource PhoneFontFamilyNormal}" FontSize="{StaticResource PhoneFontSizeNormal}"
                            Foreground="{StaticResource PhoneForegroundBrush}" SupportedOrientations="Portrait" Orientation="Portrait"
                            mc:Ignorable="d" shell:SystemTray.IsVisible="True" shell:SystemTray.Opacity="0"
                            shell:SystemTray.ForegroundColor="#fffffe" d:DesignHeight="768" d:DesignWidth="480">
    
    <!--LayoutRoot is the root grid where all page content is placed-->
    <Grid x:Name="LayoutRoot" Background="{StaticResource NoStripeBrush}">


        <Grid x:Name="LoadingPanel" Background="#7F000000">
            <StackPanel VerticalAlignment="Center" >
                <TextBlock
                    Text="{Binding LocalizedResources.Connecting, Source={StaticResource LocalizedStrings}}" TextAlignment="Center" />
                <ProgressBar x:Name="ProgressLoading" Background="Transparent" Foreground="{StaticResource PrincBrush}" />
            </StackPanel>
        </Grid>

    </Grid>

</phone:PhoneApplicationPage>