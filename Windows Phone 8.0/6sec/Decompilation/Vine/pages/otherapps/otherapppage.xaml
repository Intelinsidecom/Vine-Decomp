<phone:PhoneApplicationPage xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                            xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                            xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
                            xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
                            xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                            xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="d" d:DesignWidth="480"
                            d:DesignHeight="800" x:Class="Vine.OtherAppPage" FontFamily="{StaticResource PhoneFontFamilyNormal}"
                            FontSize="{StaticResource PhoneFontSizeNormal}" Foreground="{StaticResource PhoneForegroundBrush}"
                            Orientation="Portrait" shell:SystemTray.IsVisible="False">
    <phone:PhoneApplicationPage.Resources>
        <DataTemplate x:Key="OtherAppDataTemplate">
            <Grid Margin="0,0,0,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Image Source="{Binding Image}" Width="120" Height="120" />
                <StackPanel Grid.Column="1" HorizontalAlignment="Left" Margin="12,0,0,0" Orientation="Vertical"
                            VerticalAlignment="Top">
                    <TextBlock TextWrapping="Wrap" Text="{Binding Name}" FontFamily="Segoe WP Black" FontSize="42.667"
                               d:LayoutOverrides="Width" Margin="0,-9,0,0" />
                    <TextBlock TextWrapping="Wrap" Text="{Binding Description}" FontFamily="Segoe WP Light" FontSize="21.333"
                               d:LayoutOverrides="Width" Margin="0,-6,0,0" />
                </StackPanel>
            </Grid>
        </DataTemplate>
        <Style x:Key="ListBoxItemStyle1" TargetType="ListBoxItem">
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />

        </Style>
    </phone:PhoneApplicationPage.Resources>
   

    <!--LayoutRoot is the root grid where all page content is placed-->
    <!--ContentPanel - place additional content here-->
    <Grid x:Name="ContentPanel" Background="Black">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock TextWrapping="Wrap"
                   Text="{Binding LocalizedResources.OtherAppsInfo, Source={StaticResource LocalizedStrings}}" Margin="12,40,12,20"
                   d:LayoutOverrides="Height" FontFamily="Segoe WP Light" />
        <ListBox Grid.Row="1" ItemsSource="{Binding OtherApps}" ItemTemplate="{StaticResource OtherAppDataTemplate}"
                 ItemContainerStyle="{StaticResource ListBoxItemStyle1}" SelectionChanged="ListBox_SelectionChanged" Margin="12,0" />
        <Grid x:Name="IsLoading" Background="Black" Grid.RowSpan="2"
              Visibility="{Binding IsLoading, Converter={StaticResource VisibilityConverter}}" d:IsHidden="True">
            <ProgressBar IsIndeterminate="{Binding IsLoading}" VerticalAlignment="Center" />
        </Grid>
    </Grid>
</phone:PhoneApplicationPage>