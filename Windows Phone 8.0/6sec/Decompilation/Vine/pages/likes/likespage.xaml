<phone:PhoneApplicationPage x:Class="Vine.Pages.Likes.LikesPage"
                            xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                            xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                            xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
                            xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
                            xmlns:toolkit="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone.Controls.Toolkit"
                            xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                            xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                            FontFamily="{StaticResource PhoneFontFamilyNormal}" FontSize="{StaticResource PhoneFontSizeNormal}"
                            Foreground="{StaticResource PhoneForegroundBrush}" SupportedOrientations="Portrait" Orientation="Portrait"
                            mc:Ignorable="d" shell:SystemTray.IsVisible="True" shell:SystemTray.Opacity="0"
                            shell:SystemTray.ForegroundColor="#FF808080">
	<toolkit:TransitionService.NavigationInTransition>
		<toolkit:NavigationInTransition>

			<toolkit:NavigationInTransition.Forward>
				<toolkit:TurnstileTransition Mode="ForwardIn" />
			</toolkit:NavigationInTransition.Forward>
		</toolkit:NavigationInTransition>
	</toolkit:TransitionService.NavigationInTransition>
	<toolkit:TransitionService.NavigationOutTransition>
		<toolkit:NavigationOutTransition>

			<toolkit:NavigationOutTransition.Backward>
				<toolkit:TurnstileTransition Mode="BackwardOut" />
			</toolkit:NavigationOutTransition.Backward>
		</toolkit:NavigationOutTransition>
	</toolkit:TransitionService.NavigationOutTransition>
	<phone:PhoneApplicationPage.Resources>



		<DataTemplate x:Key="PersonDataTemplate">
			<Grid Margin="0,12,0,0">
				<Grid.ColumnDefinitions>
					<ColumnDefinition Width="*" />
					<ColumnDefinition Width="auto" />
				</Grid.ColumnDefinitions>
				<Button Style="{StaticResource SimpleButtonStyle}" Click="ViewProfile_Click">
					<Grid Margin="24,6,0,6">
						<Grid.ColumnDefinitions>
							<ColumnDefinition Width="auto" />
							<ColumnDefinition Width="*" />
						</Grid.ColumnDefinitions>

						<Border Background="{StaticResource BackgroundImageBrush}" VerticalAlignment="Top"
                                          Height="60" Width="60" >
							<Border.Clip>
								<EllipseGeometry RadiusX="30" RadiusY="30" Center="30,30"/>
							</Border.Clip>
							<Image Source="{Binding Picture}" Stretch="UniformToFill" />
						</Border>
						<StackPanel Grid.Column="1" Margin="6,0,0,0">
							<Grid HorizontalAlignment="Left">
								<Grid.ColumnDefinitions>
									<ColumnDefinition Width="auto"/>
									<ColumnDefinition Width="auto"/>
								</Grid.ColumnDefinitions>
								<TextBlock Text="{Binding Name}" Foreground="{StaticResource ThemeTextBrush}" FontSize="20" />
								<Image Source="/Assets/verifiedBadge.png" Height="19" Grid.Column="1" Stretch="Uniform" HorizontalAlignment="Left" Margin="3,2,0,0"
									Visibility="{Binding IsVerified,Converter={StaticResource VisibilityConverter}}"/>

							</Grid>
							<TextBlock Text="{Binding SubName}" Foreground="#505050" FontSize="15" />
						</StackPanel>
					</Grid>
				</Button>

				<ToggleButton Click="Following_Click" Margin="0,12,16,0" IsChecked="{Binding Following}"
											  Grid.Column="1" Style="{StaticResource FollowToggleButtonStyle}" VerticalAlignment="Top" />
			</Grid>
		</DataTemplate>

	</phone:PhoneApplicationPage.Resources>

	<!--LayoutRoot is the root grid where all page content is placed-->
	<Grid x:Name="LayoutRoot" >
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
			<RowDefinition Height="*" />
			<RowDefinition Height="Auto" />
		</Grid.RowDefinitions>

		<Rectangle x:Name="RectBackground" Grid.RowSpan="4" Fill="{StaticResource NoStripeBrush}" CacheMode="BitmapCache" />
		<Grid Grid.Row="0" Height="80" Background="{StaticResource ModernHeaderBrush}">
			<Grid Margin="24,24,0,0">
				<ContentControl HorizontalAlignment="Left" Height="25"  Style="{StaticResource LogoStyle}" Width="32"/>
				<TextBlock VerticalAlignment="Center" HorizontalAlignment="Left"
						   Foreground="{StaticResource ModernForegroundBrush}" FontSize="26.667"  Margin="40,0,0,0">
					<Run x:Name="PageTitle" FontFamily="Segoe WP Semibold"  />

					<Run x:Name="NumberTitle" FontFamily="Segoe WP Semiligh"/>
				</TextBlock>
			</Grid>
		</Grid>
		<Grid x:Name="ContentPanel"  Grid.Row="1" Grid.RowSpan="2" >
			<phone:LongListSelector x:Name="PersonsList"
                                    ItemTemplate="{StaticResource PersonDataTemplate}"
                                    ItemRealized="Likes_ItemRealized" ItemsSource="{Binding Likes}">

			</phone:LongListSelector>


		</Grid>
		<Rectangle x:Name="ShadowRectangle" Visibility="Collapsed" Grid.Row="1" IsHitTestVisible="False" Fill="{StaticResource ShadowBrush}" Height="4" VerticalAlignment="Top" />


		<Grid Grid.Row="3" x:Name="NextPanel" Visibility="Collapsed">
			<Button Content="{Binding LocalizedResources.Next, Source={StaticResource LocalizedStrings}}" Background="{StaticResource PrincBrush}" BorderThickness="0" Click="Next_Click"/>
		</Grid>


		<Grid Grid.Row="1" Visibility="{Binding IsLoading, Converter={StaticResource VisibilityConverter}}"
              VerticalAlignment="Top" Background="#7FBDC2C1">
            <ProgressBar Background="Transparent" IsIndeterminate="{Binding IsLoading}" VerticalAlignment="Top" Foreground="White" />
			<TextBlock Text="{Binding LocalizedResources.LoadingResults, Source={StaticResource LocalizedStrings}}"
                       FontSize="26.667" FontFamily="Segoe WP Light" Margin="6,6,0,6" />
		</Grid>



	</Grid>

</phone:PhoneApplicationPage>